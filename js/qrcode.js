// URL de compartilhamento (substitua pelo link desejado)
const profileUrl = "https://exemplo.com/perfil";

        // Atualiza o link de compartilhamento
    document.getElementById('profile-link').href = profileUrl;
    document.getElementById('profile-link').textContent = profileUrl;
